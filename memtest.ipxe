#!ipxe
  
set protocol http
set server_ip 192.168.1.9
set memtest_iso memtest86+-5.01.iso
  
kernel ${protocol}://${server_ip}/syslinux-6.03/memdisk || read void
initrd ${protocol}://${server_ip}/boot/${memtest_iso} || read void
imgargs memdisk iso raw || read void
boot || read void
